# Count the frequency of each element in a list:
nums = [1, 2, 3, 2, 1, 3, 2, 4, 5, 4]
frequency_count=0
for num in nums:
    if num==frequency_count:
        frequency_count+=1
    else:
     frequency_count+=1   
print(frequency_count)

